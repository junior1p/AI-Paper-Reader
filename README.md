# AI Paper Reader

智能论文阅读助手，使用 GLM-4 AI 模型，支持翻译、问答等多种功能。

## 功能特点

- 📄 上传 PDF 文献查看原文
- 🌐 使用 GLM-4 进行中英文翻译
- 💬 **基于论文内容的智能问答**
- 📖 实时翻译显示，边翻边看
- 🎨 炫酷的霓虹风格 UI
- 🔒 **Token 认证系统，防止 API 滥用**

## 快速开始

### 方式一：直接使用

1. 打开 `index.html`
2. 点击右上角 **「⚙️ 设置」**
3. 输入 API 地址和主密钥
4. 开始使用！

### 方式二：本地运行

```bash
git clone https://github.com/junior1p/AI-Paper-Reader.git
cd AI-Paper-Reader
```

直接打开 `index.html` 即可使用。

---

## 使用说明

### 配置 API

首次打开页面会自动弹出设置面板，你也可以点击右上角 **「⚙️ 设置」** 按钮随时配置：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| API 地址 | Modal 后端地址 | `https://your-app.modal.run` |
| 主密钥 | 用于获取访问 Token | `b0c6134...` （部署时生成） |

### 翻译功能

1. 点击「📁 上传 PDF」选择文献
2. 点击「🌐 翻译全文」开始翻译
3. 左右翻页查看原文和译文

### 问答功能

1. 上传 PDF 后，点击「💬 问答」标签
2. 输入问题，AI 基于论文内容回答

**问题示例：**
- "这篇文章的主要观点是什么？"
- "解释一下第3页提到的概念"
- "文章用了什么研究方法？"
- "总结一下这篇文章的结论"

---

## Modal 部署（后端 API）

### 1. 安装 Modal

```bash
pip install modal
```

### 2. 配置 Secrets

```bash
# 创建 GLM API Key
modal secret create glm-credentials GLM_API_KEY=your_glm_api_key

# 生成并保存主密钥（记下这个密钥！）
modal secret create glm-credentials MASTER_KEY=$(python -c "import secrets; print(secrets.token_hex(32))")
```

### 3. 部署到 Modal

```bash
modal deploy modal_server.py
```

部署后会显示 API URL，例如：
```
https://junioryu607--pdf-translator-fastapi-app.modal.run
```

### 4. 查看主密钥

```bash
modal secret list glm-credentials
```

找到 `MASTER_KEY` 的值，这就是你在前端设置面板中需要输入的主密钥。

---

## 安全机制

| 安全措施 | 说明 |
|----------|------|
| 主密钥分离 | 主密钥仅用于获取临时 Token，不直接调用 API |
| 临时 Token | Token 有效期 1 小时，自动刷新 |
| 请求签名 | SHA256 签名防篡改 |
| 防重放攻击 | Nonce + 时间戳验证 |
| 速率限制 | 每个 Token 每分钟最多 30 次请求 |

即使别人获取了你的 API URL，没有主密钥也无法使用！

---

## API 端点

### 获取 Token
```
POST /auth/token
{
  "master_key": "你的主密钥",
  "client_id": "web-client"  // 可选
}
```

### 翻译接口
```
POST /translate
Headers: Authorization: Bearer {token}
{
  "text": "要翻译的文本",
  "page_number": 1,
  "timestamp": 1234567890,
  "nonce": "随机字符串",
  "signature": "SHA256签名"
}
```

### 问答接口
```
POST /question
Headers: Authorization: Bearer {token}
{
  "content": "PDF 内容",
  "question": "用户问题",
  "timestamp": 1234567890,
  "nonce": "随机字符串",
  "signature": "SHA256签名"
}
```

---

## 项目结构

```
ai-paper-reader/
├── index.html          # 主页面
├── app.js              # 前端逻辑（含 Token 认证）
├── style.css           # 样式文件
├── modal_server.py     # Modal 后端服务器
├── config.example.js   # 配置示例
├── .gitignore          # Git 忽略文件
└── README.md           # 说明文档
```

## 技术栈

- **前端**: HTML + CSS + JavaScript
- **PDF 处理**: PDF.js
- **AI 模型**: GLM-4 (智谱 AI)
- **后端**: Modal + FastAPI

## License

MIT License
